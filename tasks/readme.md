## Keep track of task submissions
Since we are submitted such a small number of tasks, just keep track via submitting each as a notebook

naming structure:

```
[target]_[rangeres_meters]_[az_looks].ipynb
```